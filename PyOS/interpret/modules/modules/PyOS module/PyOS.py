# List of OS commands

def(PyOS /kernel shutdown) {
    #Shut down kernel placheolder
}

def(PyOS /kernel restart) {
    #Restart kernel placeholder
}

def(PyOS /kernel sleep) {
    #Sleep kernel placeholder
}

def(PyOS /kernel hibernate) {
    #Hibernate kernel placeholder
}

def(PyOS /interpreter load <filename>) {
    #Placeholder
}