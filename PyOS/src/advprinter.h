#indef LOG_H
#define LOG_H

#include <stdint.h>

void log(const char* message);

void print_verbose(const char* message);
	if(verbose_mode)
		try:
		try_commands(terminal_writestring(const char* message);)